package ca.prepledger.navigation;

/**
 * Contents: Contains the enum of navigable screens from within the application;
 * some screens are accessed via the sidebar, and some are sub-screens of other
 * screens.
 */
public enum ContextArea {
	RECIPES, SETTINGS, IMPORT_EXPORT, NOTIFICATIONS, PREP_LISTS, ADD_RECIPE,
	COSTING, EDIT_RECIPE, VIEW_RECIPE
}
